<form action="/submit" method="post">
    <?php
        use App\Mut\Config;
        $config = Config::getConfig();
        foreach($config['likert'] as $key=>$measured){
            echo $this->fetch('./Likert.phtml', ["measured" => [$key=>$measured]]);
        }
    ?>
    <button type="submit" class="btn btn-primary">Submit</button>
</form>