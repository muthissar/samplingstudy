db_name: './listener_db.sqlite'
sample_dir: './samples'
# sample_dir: /mnt/hel.cp.jku.at/devel/python3/DSTM/out/synthesized
likert:
    stm_struct:
        name: 'Short-Term Structure'
        desc: 'Whether the piece shows good structures in neighboring content, such as good repetitions and reasonable development'
    ltm_struct:
        name: 'Long-Term Structure'
        desc: 'Whether the piece shows good structures in long distances, such as song-level repetitions and long-distance connections'
    repetitiveness:
        name: 'Repetitiveness'
        desc: 'Whether the piece is (too) repetitive'
    # overall_preference:
    #     name: 'Overall '
    # complexity:
    # coherence: 
    #     -name: 
expertise: [
    "No",
    "Some",
    "Much"
]
